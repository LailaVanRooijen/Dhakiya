import React from "react";
import { LabelBar } from "../../components/generic/LabelBar";

const Home: React.FC = () => {
    return (
        <div>
            <LabelBar label={"Home"} />
        </div>
    );
};

export default Home;
