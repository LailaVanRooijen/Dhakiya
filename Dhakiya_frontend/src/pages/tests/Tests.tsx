import React from "react";
import { LabelBar } from "../../components/generic/LabelBar";

export const Tests = () => {
    return (
        <div>
            <LabelBar label={"Tests"} />
        </div>
    );
};
